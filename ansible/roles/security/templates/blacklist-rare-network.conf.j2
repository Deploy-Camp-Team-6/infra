# Blacklist rare and potentially dangerous network protocols
# This helps prevent certain types of network attacks

# Disable DCCP (Datagram Congestion Control Protocol)
blacklist dccp

# Disable SCTP (Stream Control Transmission Protocol)
blacklist sctp

# Disable RDS (Reliable Datagram Sockets)
blacklist rds

# Disable TIPC (Transparent Inter-process Communication)
blacklist tipc

# Disable N-hdlc (High-level Data Link Control)
blacklist n_hdlc

# Disable USB over IP
blacklist usbip-core

# Disable Firewire interfaces
blacklist firewire-ohci
blacklist firewire-sbp2

# Disable uncommon filesystems that could be used for exploitation
blacklist cramfs
blacklist freevxfs
blacklist hfs
blacklist hfsplus
blacklist jffs2
blacklist udf

# Disable Bluetooth if not needed
# Uncomment the following lines if Bluetooth is not required
# blacklist bluetooth
# blacklist btusb

# Disable uncommon serial protocols
blacklist moxa

# Disable uncommon industrial protocols
blacklist cpcctrl
blacklist mptbase
blacklist mptctl

# Prevent loading of these modules even if hardware is present
install dccp /bin/true
install sctp /bin/true
install rds /bin/true
install tipc /bin/true
install n_hdlc /bin/true
install usbip-core /bin/true
install firewire-ohci /bin/true
install firewire-sbp2 /bin/true
install cramfs /bin/true
install freevxfs /bin/true
install hfs /bin/true
install hfsplus /bin/true
install jffs2 /bin/true
install udf /bin/true
install moxa /bin/true
install cpcctrl /bin/true
install mptbase /bin/true
install mptctl /bin/true
