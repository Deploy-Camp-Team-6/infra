# Docker Swarm Service Credentials
# Generated on {{ ansible_date_time.date }} {{ ansible_date_time.time }}
# This file contains sensitive information. Keep it secure!

# Grafana
Grafana Admin Username: admin
Grafana Admin Password: {{ grafana_admin_password }}

# Portainer
Portainer Admin Username: admin
Portainer Admin Password: {{ portainer_admin_password }}

# Docker Swarm
{% if swarm_info is defined and swarm_info.docker_swarm_active and swarm_info.swarm_facts.JoinTokens is defined %}
# Worker Join Token
Worker Token: {{ swarm_info.swarm_facts.JoinTokens.Worker }}

# Manager Join Token (for additional manager nodes)
Manager Token: {{ swarm_info.swarm_facts.JoinTokens.Manager }}
{% endif %}

# To join a worker node to this swarm, run:
# docker swarm join --token <worker-token> {{ docker_swarm_addr }}:{{ docker_swarm_port }}

# To join a manager node to this swarm, run:
# docker swarm join --token <manager-token> {{ docker_swarm_addr }}:{{ docker_swarm_port }}
